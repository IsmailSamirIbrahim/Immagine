dist: trusty
sudo: false
language: cpp

script:
   script:
         - cmake --version
         - mkdir build
         - cd build
         - cmake .. -G "Visual Studio 15 Win64"
         - cmake --build . --config Debug
         - cmake --build . --config Release